{
  "name": "@decocms/mesh",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "bun run --filter=mcp-mesh --elide-lines=0 dev",
    "fmt": "biome format --write",
    "lint": "oxlint",
    "check": "tsc --noEmit",
    "test": "vitest run",
    "fmt:check": "biome format",
    "test:watch": "vitest",
    "docs:dev": "cd docs && npm run dev",
    "docs:deploy": "cd docs && npm run deploy",
    "build:runtime": "bun run --filter=@decocms/runtime build",
    "ui": "npx shadcn@latest add -c ./packages/ui",
    "clean": "rm -f deno.lock package-lock.json bun.lock && rm -rf node_modules apps/*/node_modules packages/*/node_modules",
    "knip": "knip"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "devDependencies": {
    "@biomejs/biome": "2.2.5",
    "@types/node": "^24.9.1",
    "knip": "^5.66.2",
    "oxlint": "1.23.0",
    "typescript": "^5.9.3",
    "vitest": "3.2.4",
    "react": "^19.2.0"
  },
  "overrides": {
    "zod-to-json-schema": "3.25.0"
  },
  "engines": {
    "node": ">=24.0.0"
  }
}
