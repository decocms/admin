---
title: Introdução
description: Conheça o deco CMS - o Sistema de Gerenciamento de Contexto para software nativo de IA
icon: Rocket
---

![deco](https://assets.decocache.com/mcp/9a3facd8-1199-4635-becc-bdc3f5ee7ab1/deco-banner-capy.png)

## O que é deco CMS?

**Deco CMS** é um Sistema de Gerenciamento de Contexto open-source, o MCP Mesh para IA. Ele permite que equipes construam, implantem e gerenciem aplicações nativas de IA através de colaboração única entre usuários de negócios e desenvolvedores.

> Pense em **Lovable + n8n + LangGraph**, rodando no Cloudflare com um único comando de deploy.  
> IA full-stack, pronta para produção.

## Proposta de Valor

deco CMS conecta protótipos de IA a sistemas de produção:

- **Para AI Builders:** Interface de construção em linguagem natural para usuários de negócios criarem e implantarem apps de IA sem código
- **Para Context Engineers:** SDK TypeScript open-source para desenvolvedores estenderem, personalizarem e hospedarem em sua própria infraestrutura
- **Colaboração Fluida:** Ambos os perfis trabalham juntos na mesma plataforma com contexto compartilhado

## Arquitetura em Resumo

deco CMS é construído sobre duas camadas fundamentais:

### 1. MCP Mesh (O Kernel)

A espinha dorsal que gerencia contexto, conexões e observabilidade para cada agente no seu sistema.

- **Componha e proteja MCPs** em toda a sua organização
- **Conecte e proxie MCPs externos** com tokens seguros
- **Exponha Virtual MCPs governados** ("AI Apps") para qualquer cliente MCP
- **Aplique auth/RBAC/audit/FinOps** em cada camada
- **Obtenha observabilidade completa** em toda a stack

### 2. AI App Framework (Virtual MCPs no Mesh)

Construa software web nativo de IA que chama tools:

- **Full-stack**: Do banco de dados à UI com React 19 + Tailwind v4
- **Generative Admin**: _deco chat_ ajuda você a escrever PRDs e implementar
- **Admin, workflows e views customizadas** governadas pelo Mesh

## Blocos de Construção Principais

### Tools

Funções que executam ações como chamadas de API, consultas ao banco de dados ou computações.

```typescript
createTool({
  id: "CEP_SEARCH",
  description: "Buscar CEPs brasileiros",
  inputSchema: z.object({ cep: z.string() }),
  outputSchema: z.object({ city: z.string(), state: z.string() }),
  execute: async ({ context }) => { /* ... */ },
});
```

### Agents

Assistentes de IA que entendem linguagem natural e chamam tools. Customizáveis com:
- Seleção de modelo (GPT-4, Claude, etc.)
- System prompts
- Acesso a tools
- Contexto de recursos

### Workflows

Sequências determinísticas de tools para automação. Diferente de agents que decidem, workflows seguem passos predefinidos.

```typescript
createWorkflow({ id: "REGISTER_CUSTOMER" })
  .then(validateInput)
  .then(saveToDatabase) 
  .then(sendWelcomeEmail)
  .commit();
```

### Views

Interfaces React customizadas para seus tools e workflows. Construídas com React 19 + Tailwind v4.

### Documents

Conteúdo Markdown (PRDs, guias, notas) anexável a agents / qualquer thread para contexto.

### Resources

Tudo no deco CMS é um resource: tools, agents, workflows, views e documents. Resources são:
- Tipados com schemas Zod
- Armazenados no deconfig (versionamento tipo-git)
- Referenciáveis usando menções `@` em threads e system prompts de agents

### Database

SQLite integrado com Drizzle ORM para persistência de dados.

### MCP Mesh

Camada de agregação de contexto que unifica integrações, tools e políticas:
- Proxy MCP externo
- Armazenamento seguro de credenciais
- Roteamento RBAC e políticas
- Rastreamento OpenTelemetry
- Tracking de custos

## Para quem é o deco CMS?

**AI Builders:** Product managers, equipes de ops, analistas de negócios construindo apps sem código

**Context Engineers:** Desenvolvedores estendendo com TypeScript, auto-hospedando, construindo integrações customizadas

## Por que deco CMS?

Equipes de IA estão presas entre protótipos low-code e caos de produção. Backends em n8n ou LangGraph. Frontends em Lovable ou React. Deploys separados. Auth inconsistente. Custos em espiral.

**deco CMS unifica tudo:**

- 🧠 **MCP-nativo**, Componha servidores com policy, auth e observabilidade integrados
- ⚙️ **TypeScript full-stack**, Agents, workflows, UIs em um repo
- 🌍 **Deploy em qualquer lugar**, Cloudflare Workers, AWS ou runtimes locais
- 🔐 **Governança integrada**, RBAC, audit trails, caps de gastos
- 🔭 **Observabilidade unificada**, Rastreie UI → agents → models
- 🧩 **Aberto & modular**, Instale da deco store ou construa o seu próprio

---

Pronto para construir seu primeiro AI app? Escolha seu caminho:

- **[Para AI Builders](/pt-br/getting-started/ai-builders)** - Construa sem código
- **[Para Context Engineers](/pt-br/getting-started/context-engineers)** - Desenvolva com TypeScript
