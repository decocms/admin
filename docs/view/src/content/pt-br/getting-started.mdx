---
title: Primeiros Passos
description: Crie e faça deploy do seu primeiro servidor MCP em minutos
icon: Play
---

import { StepGroup } from "../../components/ui/StepGroup";
import { Step } from "../../components/ui/Step";
import Callout from "../../components/ui/Callout.astro";

## Pré-requisitos

Antes de começar, certifique-se de ter:

- **Node.js** v18+ ([baixar aqui](https://nodejs.org))
- **Git** para controle de versão
- **Editor de código** (recomendamos [Cursor](https://cursor.com) para desenvolvimento com IA)
- **Conhecimento básico de TypeScript** – O projeto usa muito TypeScript, então familiaridade com TS é importante (se você é iniciante, veja a documentação oficial do TypeScript para começar).

## Configuração do Projeto com a CLI

<StepGroup>
  <Step number={1} title="Instalar Deco CLI">
    ```bash
    npm i -g deco-cli
    ```
    
    Verifique a instalação com `deco --help`.
  </Step>

  <Step number={2} title="Login no deco.chat">
    ```bash
    deco login
    ```
    
    Isso abre seu navegador para autenticação. Crie uma conta em [deco.chat](https://deco.chat) se você não tiver uma.
  </Step>

  <Step number={3} title="Criar seu projeto">
    ```bash
    deco create my-mcp-project
    ```
    
    - Escolha seu time/workspace
    - Selecione o template padrão (recomendado)
    - Aceite a inicialização do repositório Git
    - Configure o setup do IDE
  </Step>

  <Step number={4} title="Iniciar desenvolvimento">
    ```bash
    cd my-mcp-project
    npm install
    npm run dev
    ```
    
    Seu servidor MCP vai iniciar com hot reload habilitado. Copie a URL de preview da saída do terminal.
    
    <Callout type="info">
      O servidor roda na porta 8787 e serve tanto endpoints MCP em `/mcp` quanto assets da documentação.
    </Callout>
  </Step>
</StepGroup>

## Testar seu MCP

Teste seu servidor MCP no deco.chat:

1. Vá para seu time em [deco.chat](https://deco.chat)
2. Clique em **Add Integration** → **Custom Integration**
3. Cole sua URL de preview + `/mcp` (ex: `https://localhost-abc123.deco.host/mcp`)
4. Salve e teste as ferramentas padrão

<Callout type="success">
  Seu servidor MCP agora está rodando localmente e conectado ao deco.chat! 🎉
</Callout>

## Deploy para produção

Quando estiver pronto para compartilhar seu MCP:

```bash
npm run deploy
```

Seu app faz deploy no Cloudflare Workers e fica disponível como uma integração pública no seu workspace.