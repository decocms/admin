import { Integration } from "../../../../../packages/sdk/src/models/index.ts";

export function subscribeToInstall(
  installId: string,
  onUpdate: (install: Integration) => void,
) {
  INTEGRATION_CHANNEL;
}
