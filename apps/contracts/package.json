{
  "name": "contract",
  "version": "1.0.0",
  "scripts": {
    "dev": "deco dev -l",
    "deploy": "wrangler build && deco deploy -y ./dist",
    "gen": "deco gen > deco.gen.ts"
  },
  "dependencies": {
    "@deco/workers-runtime": "file:../../packages/runtime",
    "zod": "^3.24.3"
  },
  "devDependencies": {
    "wrangler": "^4.28.0"
  }
}
